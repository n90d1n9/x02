name: ky_sheet_mcp_server
description: MCP Server for KySheet spreadsheet operations
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  mcp_server: ^0.1.0
  mcp_server_annotations: ^0.1.0
  json_rpc_2: ^3.0.0
  stream_channel: ^2.1.0
  # Local dependencies to ky_sheet
  ky_sheet:
    path: ../
  
dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
  build_runner: ^2.4.0
  mcp_server_generator: ^0.1.0
