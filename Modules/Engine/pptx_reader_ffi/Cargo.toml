[package]
name = "pptx_reader_ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
pptx_reader = { path = "../pptx_reader" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
zip = "0.6"
base64 = "0.21"
