import 'package:flutter/material.dart';

class DocumentPageRuler extends StatelessWidget {
  const DocumentPageRuler({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
